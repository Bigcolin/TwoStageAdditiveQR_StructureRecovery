include("../src/model.ji")
include("../src/StructClassifier.ji")
var = 1
eta = 0.
q = 0.5
nSamples = 100
dSamples = 3
d_zeros = 1
err_type = "g"
f = open("record.txt", "w")
close(f)
@threads for k in 1:10
	trainData = additiveQR.data(nSamples, dSamples, d_zeros, var, eta, err_type)
	model1 = model(trainData, 0.5)
	res1 = strucRec(model1, 2, 12.0, 20)
    f = open("record.txt", "a")
	println(f, res1, "\n")
    close(f)
end

# sae(l) = additiveQR.SAE(model1, l, 1)
